{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "shell",
      "label": "Build All Target",
      "command": "/usr/local/bin/cmake --build ${workspaceFolder}/build --config Debug --target all --",
      "group": "none"
    }
  ]
}
